{
  "name": "nexus-desktop",
  "private": true,
  "version": "3.2.0",
  "main": "electron/main.js",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080",
    "build": "vite build",
    "preview": "vite preview",
    "electron:dev": "electron .",
    "desktop": "electron ."
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "vite": "^5.4.0"
  }
}
